# Copy this file to backend/.env and fill in your credentials.
# This file is checked into the repo as an example and should NOT contain real secrets.

# Google Gemini API key (OAuth2 access token / service account token depending on setup)
GEMINI_API_KEY=

# Optional: model name
GEMINI_MODEL=models/text-bison-001

# Eleven Labs API key for text-to-speech
ELEVENLABS_API_KEY=

# JWT Secret for authentication
JWT_SECRET=your-secret-key-here
